version: "3.2"

services:
    db:
        image: mongo
        tty: true
        ports:
            - "27017:27017"
    web:
        build: web
        tty: true
        ports:
            - "80:80"
        links:
            - db
        depends_on:
            - db
        command: ""
        restart: on-failure
